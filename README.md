# GX Types

Adds custom types for inbuilt GM resources

## Building
Requirements:
- Node.JS v22 (earlier/later versions may work)
- npm 11 (earlier/later versions may work)

Steps:
1. Open terminal window in `extensions/gx-types/gx-types` folder
2. Run `npm install`
3. Run `npm run build`


